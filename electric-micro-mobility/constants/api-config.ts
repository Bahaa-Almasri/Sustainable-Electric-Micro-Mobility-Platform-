/**
 * Backend API base URL — set via EXPO_PUBLIC_API_URL (.env) or app.config.js `extra.apiBaseUrl`.
 * Android emulator: use http://10.0.2.2:8000  (localhost on host)
 * iOS simulator: http://127.0.0.1:8000
 */
export const API_BASE_URL_OVERRIDE = '';
